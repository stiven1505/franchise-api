$kind: http-request
url: "{{baseUrl}}/api/v1/franchises/{{franchiseId}}/branches/{{branchId}}/products/{{productId}}/name"
method: PATCH
body:
  type: json
  content: |-
    {
        "name": "Big Mac Deluxe"
    }
order: 9000

$kind: http-request
url: "{{baseUrl}}/api/v1/franchises/{{franchiseId}}/branches/{{branchId}}/name"
method: PATCH
body:
  type: json
  content: |-
    {
        "name": "Sucursal Principal Norte"
    }
order: 8000
